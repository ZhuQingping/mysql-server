# PQ supports sysdate(change to now) when --sysdate-is-now is on
--source include/pq_test.inc
 
create table t(a int);
insert into t values(1),(2);
explain select a, sysdate() from t;
--replace_column 2 #
select a, sysdate() from t;
 
drop table t;
