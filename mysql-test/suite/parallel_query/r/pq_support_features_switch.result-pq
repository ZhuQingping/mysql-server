set @saved_force_parallel_execute = @@force_parallel_execute;
set @saved_parallel_cost_threshold = @@parallel_cost_threshold;
set @saved_parallel_rows_threshold = @@parallel_cost_threshold;
set @saved_sql_mode = @@sql_mode;
# 1. variables test
set force_parallel_execute = off;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	OFF
set force_parallel_execute = on;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set force_parallel_execute = auto;
ERROR 42000: Variable 'force_parallel_execute' can't be set to the value of 'auto'
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set force_parallel_execute = 'true';
ERROR 42000: Variable 'force_parallel_execute' can't be set to the value of 'true'
set force_parallel_execute = true;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set force_parallel_execute = false;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	OFF
set force_parallel_execute = 0;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	OFF
set force_parallel_execute = 1;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set force_parallel_execute = 2;
ERROR 42000: Variable 'force_parallel_execute' can't be set to the value of '2'
set global force_parallel_execute = off;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set global force_parallel_execute = on;
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set global force_parallel_execute = auto;
ERROR 42000: Variable 'force_parallel_execute' can't be set to the value of 'auto'
show variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	ON
set global force_parallel_execute = default;
show global variables like 'force_parallel_execute';
Variable_name	Value
force_parallel_execute	OFF
select /*+ set_var(force_parallel_execute=on) */ @@force_parallel_execute;
@@force_parallel_execute
1
select /*+ set_var(force_parallel_execute=off) */ @@force_parallel_execute;
@@force_parallel_execute
0
SET @saved_pq_support_features_switch = @@global.pq_support_features_switch;
set pq_support_features_switch = default;
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'simple_aggregate=on';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'simple_aggregate=off';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=off,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'simple_aggregate=default';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'simple_aggregate=on';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'simple_aggregate=off';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=off,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'simple_aggregate=default';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'correlated_subquery=on';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'correlated_subquery=off';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=off,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'correlated_subquery=default';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'correlated_subquery=on';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'correlated_subquery=off';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=off,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'correlated_subquery=default';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'hash_join_spill_to_disk=on';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'hash_join_spill_to_disk=off';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=on
set pq_support_features_switch = 'hash_join_spill_to_disk=default';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'hash_join_spill_to_disk=on';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'hash_join_spill_to_disk=off';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=on
set global pq_support_features_switch = 'hash_join_spill_to_disk=default';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=on
set pq_support_features_switch = 'insert_select=on';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'insert_select=off';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=off
set pq_support_features_switch = 'insert_select=default';
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set global pq_support_features_switch = 'insert_select=on';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=on
set global pq_support_features_switch = 'insert_select=off';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=off
set global pq_support_features_switch = 'insert_select=default';
show global variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=off,insert_select=off
select /*+ set_var(pq_support_features_switch='simple_aggregate=on') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
select /*+ set_var(pq_support_features_switch='simple_aggregate=off') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=off,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
select /*+ set_var(pq_support_features_switch='count_distinct=on') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
select /*+ set_var(pq_support_features_switch='correlated_subquery=on') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
select /*+ set_var(pq_support_features_switch='hash_join_spill_to_disk=on') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
select /*+ set_var(pq_support_features_switch='insert_select=on') */ @@pq_support_features_switch;
@@pq_support_features_switch
simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 0;
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=off,count_distinct=off,correlated_subquery=off,hash_join_spill_to_disk=off,insert_select=off
set pq_support_features_switch = 1;
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=off,correlated_subquery=off,hash_join_spill_to_disk=off,insert_select=off
set pq_support_features_switch = 31;
show variables like '%pq_support_features_switch%';
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
set pq_support_features_switch = 'simple_agg=on';
ERROR 42000: Variable 'pq_support_features_switch' can't be set to the value of 'simple_agg=on'
# 2. Test simple_aggregate optimization in pq_support_features_switch
set session parallel_cost_threshold = 0;
set session parallel_rows_threshold = 0;
set session force_parallel_execute = off;
# aggregate with single Query
create table t1(a int primary key, b varchar(10), c decimal(5,2));
create table t2(a int primary key, b varchar(10), c decimal(5,2));
insert into t1 values (1, 'The', '3.14');
insert into t1 values (2, 'quick', '4.25');
insert into t1 values (3, 'brown', '5.36');
insert into t1 values (4, 'fox', '6.47');
insert into t1 values (5, 'jumps', '3.14');
insert into t1 values (6, 'over', '4.25');
insert into t1 values (7, 'the', '5.36');
insert into t1 values (8, 'lazy', '6.47');
insert into t1 values (9, 'dog', '7.58');
insert into t2 select * from t1 where a > 3;
analyze table t1;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
analyze table t2;
Table	Op	Msg_type	Msg_text
test.t2	analyze	status	OK
explain select count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select count(*), sum(c) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)`,sum(`test`.`t1`.`c`) AS `sum(c)` from `test`.`t1`
explain select count(*) from t1 where c > 5.0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Using where
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where (`test`.`t1`.`c` > 5.00)
explain select count(*) as cnt from t1 order by cnt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `cnt` from `test`.`t1`
explain select count(*) as g from t1 order by a limit 1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `g` from `test`.`t1` limit 1
explain select count(*) from t1 group by(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Using temporary
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`b`
explain select a, count(*) from t1 group by a order by b;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	9	100.00	Using temporary; Using filesort
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`a` order by `test`.`t1`.`b`
explain select count(*) from t1 join t2 on t1.a = t2.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t2	NULL	index	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
1	SIMPLE	t1	NULL	eq_ref	PRIMARY	PRIMARY	4	test.t2.a	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` join `test`.`t2` where (`test`.`t1`.`a` = `test`.`t2`.`a`)
explain select distinct count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select a from t1 having a > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using where; Using index
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` having (`test`.`t1`.`a` > 3) order by `test`.`t1`.`a` limit 10
explain select sum(a+2) as SUM from t1 having SUM > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum((`test`.`t1`.`a` + 2)) AS `SUM` from `test`.`t1` having (`SUM` > 3)
explain select count(a) from t1 having sum(a) > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(a)` from `test`.`t1` having (sum(`test`.`t1`.`a`) > 3) limit 10
explain select sum(distinct t1.a) from t1 where t1.a > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	range	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`a`) AS `sum(distinct t1.a)` from `test`.`t1` where (`test`.`t1`.`a` > 3)
drop table t1,t2;
# subquery
create table t1 (id int, a int);
create table t2 (b int);
insert into t1 values (1, 1), (2, 1), (0, 2), (1, 3), (3, 3), (0, 2);
insert into t2 values (1), (3), (2), (0), (1), (2), (1);
analyze table t1, t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select id, (select avg(b) from t2) as t from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,(/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`) AS `t` from `test`.`t1`
explain select sum(t1.a) from t1, t2 group by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
1	PRIMARY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Using join buffer (hash join)
2	SUBQUERY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `sum(t1.a)` from `test`.`t1` join `test`.`t2` group by ((/* select#2 */ select avg(`test`.`t1`.`id`) from `test`.`t1`) + `test`.`t1`.`a`)
explain select t1.a from t1 order by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using filesort
2	SUBQUERY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` order by ((/* select#2 */ select avg(`test`.`t1`.`id`) from `test`.`t1`) + `test`.`t1`.`a`)
explain select * from t1 where a > (select avg(b) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a` from `test`.`t1` where (`test`.`t1`.`a` > (/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`))
explain select id, sum((select avg(b) from t2)) as t from t1 group by id order by id;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary; Using filesort
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,sum((/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`)) AS `t` from `test`.`t1` group by `test`.`t1`.`id` order by `test`.`t1`.`id`
explain SELECT SUM(a), (SELECT SUM(b) FROM t2) AS scalar FROM t1 HAVING SUM(a) > scalar;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `SUM(a)`,(/* select#2 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `scalar` from `test`.`t1` having (sum(`test`.`t1`.`a`) > `scalar`)
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1  HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<derived2>	NULL	system	NULL	NULL	NULL	NULL	1	100.00	NULL
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	DERIVED	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
3	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,'12' AS `c1`,'10' AS `c2` from `test`.`t1`
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1 group by t1.a HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
1	PRIMARY	<derived2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using join buffer (hash join)
2	DERIVED	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
3	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1` join (/* select#2 */ select sum(`test`.`t1`.`a`) AS `c1`,(/* select#3 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `c2` from `test`.`t1` group by `test`.`t1`.`a` having (sum(`test`.`t1`.`a`) > `c2`)) `dt`
explain select sum(distinct t1.id) from t1 where t1.id > (select t1.a from t1 limit 1);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`id`) AS `sum(distinct t1.id)` from `test`.`t1` where (`test`.`t1`.`id` > (/* select#2 */ select `test`.`t1`.`a` from `test`.`t1` limit 1))
drop tables t1, t2;
create table t1(a int not null, b char(10) not null);
create table t2(a int not null, b char(10) not null);
insert into t1 values (1, 'a'), (2, 'b'), (3, 'c'), (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'),(8,'h'),(0,'j');
insert into t2 values (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'), (1, 'a'), (2, 'b'), (7, 'g'),(0,'i');
analyze table t1,t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select * from t1 where exists(select * from t2 where t2.b=t1.b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Start temporary
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Using where; End temporary; Using join buffer (hash join)
Warnings:
Note	1276	Field or reference 'test.t1.b' of SELECT #2 was resolved in SELECT #1
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` semi join (`test`.`t2`) where (`test`.`t1`.`b` = `test`.`t2`.`b`)
explain select * from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` where true
explain select count(*) from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where true
drop table t1,t2;
# view
create table small (id INTEGER not null, PRIMARY KEY (id));
create table big (id INTEGER not null, PRIMARY KEY (id));
insert into small VALUES (1), (2);
insert into big VALUES (1), (2), (3), (4);
create view small_view AS SELECT *, IF (id % 2 = 1, 1, 0) AS is_odd FROM small;
create view big_view AS SELECT big.*, small_view.id AS small_id, small_view.is_odd FROM big LEFT JOIN small_view ON small_view.id = big.id;
explain select count(*) from small_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	small	NULL	index	NULL	PRIMARY	4	NULL	2	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`small`
explain select count(*) from big_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	big	NULL	index	NULL	PRIMARY	4	NULL	4	100.00	Using index
1	SIMPLE	small	NULL	eq_ref	PRIMARY	PRIMARY	4	test.big.id	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`big` left join (`test`.`small`) on((`test`.`small`.`id` = `test`.`big`.`id`)) where true
drop table small, big;
drop view small_view, big_view;
# insert select
set pq_support_features_switch='insert_select=on';
create table t0 (a int);
create table t1 (a int);
insert into t0 values (1),(2),(3);
insert into t1 values (1),(2),(3);
explain insert into t1 select * from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select `test`.`t0`.`a` AS `a` from `test`.`t0`
explain insert into t1 select count(*) from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select count(0) AS `count(*)` from `test`.`t0`
drop table t0,t1;
# PQ_HINT
create table t1(a int primary key, b varchar(10), c decimal(5,2));
explain select/*+ PQ(2) */ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select/*+ PQ(2) */ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
drop table t1;
set session force_parallel_execute = on;
# aggregate with single Query
create table t1(a int primary key, b varchar(10), c decimal(5,2));
create table t2(a int primary key, b varchar(10), c decimal(5,2));
insert into t1 values (1, 'The', '3.14');
insert into t1 values (2, 'quick', '4.25');
insert into t1 values (3, 'brown', '5.36');
insert into t1 values (4, 'fox', '6.47');
insert into t1 values (5, 'jumps', '3.14');
insert into t1 values (6, 'over', '4.25');
insert into t1 values (7, 'the', '5.36');
insert into t1 values (8, 'lazy', '6.47');
insert into t1 values (9, 'dog', '7.58');
insert into t2 select * from t1 where a > 3;
analyze table t1;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
analyze table t2;
Table	Op	Msg_type	Msg_text
test.t2	analyze	status	OK
explain select count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select count(*), sum(c) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)`,sum(`test`.`t1`.`c`) AS `sum(c)` from `test`.`t1`
explain select count(*) from t1 where c > 5.0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Using where
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where (`test`.`t1`.`c` > 5.00)
explain select count(*) as cnt from t1 order by cnt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `cnt` from `test`.`t1`
explain select count(*) as g from t1 order by a limit 1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `g` from `test`.`t1` limit 1
explain select count(*) from t1 group by(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Using temporary
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`b`
explain select a, count(*) from t1 group by a order by b;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	9	100.00	Using temporary; Using filesort
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`a` order by `test`.`t1`.`b`
explain select count(*) from t1 join t2 on t1.a = t2.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t2)
1	SIMPLE	t2	NULL	index	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
1	SIMPLE	t1	NULL	eq_ref	PRIMARY	PRIMARY	4	test.t2.a	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` join `test`.`t2` where (`test`.`t1`.`a` = `test`.`t2`.`a`)
explain select distinct count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select a from t1 having a > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using where; Using index
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` having (`test`.`t1`.`a` > 3) order by `test`.`t1`.`a` limit 10
explain select sum(a+2) as SUM from t1 having SUM > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum((`test`.`t1`.`a` + 2)) AS `SUM` from `test`.`t1` having (`SUM` > 3)
explain select count(a) from t1 having sum(a) > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(a)` from `test`.`t1` having (sum(`<temporary>`.`sum(t1.a)`) > 3) limit 10
explain select sum(distinct t1.a) from t1 where t1.a > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	range	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`a`) AS `sum(distinct t1.a)` from `test`.`t1` where (`test`.`t1`.`a` > 3)
drop table t1,t2;
# subquery
create table t1 (id int, a int);
create table t2 (b int);
insert into t1 values (1, 1), (2, 1), (0, 2), (1, 3), (3, 3), (0, 2);
insert into t2 values (1), (3), (2), (0), (1), (2), (1);
analyze table t1, t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select id, (select avg(b) from t2) as t from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,(/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`) AS `t` from `test`.`t1`
explain select sum(t1.a) from t1, t2 group by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t1, test.t2)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Using join buffer (hash join)
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `sum(t1.a)` from `test`.`t1` join `test`.`t2` group by `((select #2) + t1.a)`
explain select t1.a from t1 order by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using filesort
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` order by `((select #2) + t1.a)`
explain select * from t1 where a > (select avg(b) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a` from `test`.`t1` where (`test`.`t1`.`a` > (/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`))
explain select id, sum((select avg(b) from t2)) as t from t1 group by id order by id;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary; Using filesort
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,sum((/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`)) AS `t` from `test`.`t1` group by `test`.`t1`.`id` order by `test`.`t1`.`id`
explain SELECT SUM(a), (SELECT SUM(b) FROM t2) AS scalar FROM t1 HAVING SUM(a) > scalar;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `SUM(a)`,(/* select#2 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `scalar` from `test`.`t1` having (sum(`<temporary>`.`sum(t1.a)`) > `scalar`)
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1  HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	<derived2>	NULL	system	NULL	NULL	NULL	NULL	1	100.00	NULL
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	DERIVED	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
3	SUBQUERY	<gather3>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
3	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1`
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1 group by t1.a HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
1	SIMPLE	<derived2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using join buffer (hash join)
2	DERIVED	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
3	SUBQUERY	<gather3>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
3	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1` join (/* select#2 */ select sum(`test`.`t1`.`a`) AS `c1`,(/* select#3 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `c2` from `test`.`t1` group by `test`.`t1`.`a` having (sum(`<temporary>`.`a`) > `c2`)) `dt`
explain select sum(distinct t1.id) from t1 where t1.id > (select t1.a from t1 limit 1);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`id`) AS `sum(distinct t1.id)` from `test`.`t1` where (`test`.`t1`.`id` > (/* select#2 */ select `test`.`t1`.`a` from `test`.`t1` limit 1))
drop tables t1, t2;
create table t1(a int not null, b char(10) not null);
create table t2(a int not null, b char(10) not null);
insert into t1 values (1, 'a'), (2, 'b'), (3, 'c'), (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'),(8,'h'),(0,'j');
insert into t2 values (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'), (1, 'a'), (2, 'b'), (7, 'g'),(0,'i');
analyze table t1,t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select * from t1 where exists(select * from t2 where t2.b=t1.b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Parallel execute (4 workers, test.t1)
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Start temporary
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Using where; End temporary; Using join buffer (hash join)
Warnings:
Note	1276	Field or reference 'test.t1.b' of SELECT #2 was resolved in SELECT #1
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` semi join (`test`.`t2`) where (`test`.`t1`.`b` = `test`.`t2`.`b`)
explain select * from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` where true
explain select count(*) from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where true
drop table t1,t2;
# view
create table small (id INTEGER not null, PRIMARY KEY (id));
create table big (id INTEGER not null, PRIMARY KEY (id));
insert into small VALUES (1), (2);
insert into big VALUES (1), (2), (3), (4);
create view small_view AS SELECT *, IF (id % 2 = 1, 1, 0) AS is_odd FROM small;
create view big_view AS SELECT big.*, small_view.id AS small_id, small_view.is_odd FROM big LEFT JOIN small_view ON small_view.id = big.id;
explain select count(*) from small_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	2	100.00	Parallel execute (4 workers, test.small)
1	SIMPLE	small	NULL	index	NULL	PRIMARY	4	NULL	2	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`small`
explain select count(*) from big_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	4	100.00	Parallel execute (4 workers, test.big)
1	SIMPLE	big	NULL	index	NULL	PRIMARY	4	NULL	4	100.00	Using index
1	SIMPLE	small	NULL	eq_ref	PRIMARY	PRIMARY	4	test.big.id	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`big` left join (`test`.`small`) on((`test`.`small`.`id` = `test`.`big`.`id`)) where true
drop table small, big;
drop view small_view, big_view;
# insert select
set pq_support_features_switch='insert_select=on';
create table t0 (a int);
create table t1 (a int);
insert into t0 values (1),(2),(3);
insert into t1 values (1),(2),(3);
explain insert into t1 select * from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	Parallel execute (4 workers, test.t0)
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select `test`.`t0`.`a` AS `a` from `test`.`t0`
explain insert into t1 select count(*) from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	Parallel execute (4 workers, test.t0)
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select count(0) AS `count(*)` from `test`.`t0`
drop table t0,t1;
# PQ_HINT
create table t1(a int primary key, b varchar(10), c decimal(5,2));
explain select/*+ PQ(2) */ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select/*+ PQ(2) */ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
drop table t1;
set session pq_support_features_switch='simple_aggregate=off';
# aggregate with single Query
create table t1(a int primary key, b varchar(10), c decimal(5,2));
create table t2(a int primary key, b varchar(10), c decimal(5,2));
insert into t1 values (1, 'The', '3.14');
insert into t1 values (2, 'quick', '4.25');
insert into t1 values (3, 'brown', '5.36');
insert into t1 values (4, 'fox', '6.47');
insert into t1 values (5, 'jumps', '3.14');
insert into t1 values (6, 'over', '4.25');
insert into t1 values (7, 'the', '5.36');
insert into t1 values (8, 'lazy', '6.47');
insert into t1 values (9, 'dog', '7.58');
insert into t2 select * from t1 where a > 3;
analyze table t1;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
analyze table t2;
Table	Op	Msg_type	Msg_text
test.t2	analyze	status	OK
explain select count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select count(*), sum(c) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)`,sum(`test`.`t1`.`c`) AS `sum(c)` from `test`.`t1`
explain select count(*) from t1 where c > 5.0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Using where
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where (`test`.`t1`.`c` > 5.00)
explain select count(*) as cnt from t1 order by cnt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `cnt` from `test`.`t1`
explain select count(*) as g from t1 order by a limit 1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `g` from `test`.`t1` limit 1
explain select count(*) from t1 group by(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Using temporary
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`b`
explain select a, count(*) from t1 group by a order by b;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	9	100.00	Using temporary; Using filesort
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`a` order by `test`.`t1`.`b`
explain select count(*) from t1 join t2 on t1.a = t2.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t2)
1	SIMPLE	t2	NULL	index	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
1	SIMPLE	t1	NULL	eq_ref	PRIMARY	PRIMARY	4	test.t2.a	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` join `test`.`t2` where (`test`.`t1`.`a` = `test`.`t2`.`a`)
explain select distinct count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select a from t1 having a > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using where; Using index
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` having (`test`.`t1`.`a` > 3) order by `test`.`t1`.`a` limit 10
explain select sum(a+2) as SUM from t1 having SUM > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum((`test`.`t1`.`a` + 2)) AS `SUM` from `test`.`t1` having (`SUM` > 3)
explain select count(a) from t1 having sum(a) > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(a)` from `test`.`t1` having (sum(`test`.`t1`.`a`) > 3) limit 10
explain select sum(distinct t1.a) from t1 where t1.a > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	range	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`a`) AS `sum(distinct t1.a)` from `test`.`t1` where (`test`.`t1`.`a` > 3)
drop table t1,t2;
# subquery
create table t1 (id int, a int);
create table t2 (b int);
insert into t1 values (1, 1), (2, 1), (0, 2), (1, 3), (3, 3), (0, 2);
insert into t2 values (1), (3), (2), (0), (1), (2), (1);
analyze table t1, t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select id, (select avg(b) from t2) as t from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,(/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`) AS `t` from `test`.`t1`
explain select sum(t1.a) from t1, t2 group by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t1, test.t2)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Using join buffer (hash join)
2	SUBQUERY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `sum(t1.a)` from `test`.`t1` join `test`.`t2` group by `((select #2) + t1.a)`
explain select t1.a from t1 order by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using filesort
2	SUBQUERY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` order by `((select #2) + t1.a)`
explain select * from t1 where a > (select avg(b) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a` from `test`.`t1` where (`test`.`t1`.`a` > (/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`))
explain select id, sum((select avg(b) from t2)) as t from t1 group by id order by id;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary; Using filesort
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,sum((/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`)) AS `t` from `test`.`t1` group by `test`.`t1`.`id` order by `test`.`t1`.`id`
explain SELECT SUM(a), (SELECT SUM(b) FROM t2) AS scalar FROM t1 HAVING SUM(a) > scalar;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `SUM(a)`,(/* select#2 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `scalar` from `test`.`t1` having (sum(`test`.`t1`.`a`) > `scalar`)
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1  HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	<derived2>	NULL	system	NULL	NULL	NULL	NULL	1	100.00	NULL
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	DERIVED	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
3	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1`
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1 group by t1.a HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
1	SIMPLE	<derived2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using join buffer (hash join)
2	DERIVED	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
3	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1` join (/* select#2 */ select sum(`test`.`t1`.`a`) AS `c1`,(/* select#3 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `c2` from `test`.`t1` group by `test`.`t1`.`a` having (sum(`<temporary>`.`a`) > `c2`)) `dt`
explain select sum(distinct t1.id) from t1 where t1.id > (select t1.a from t1 limit 1);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`id`) AS `sum(distinct t1.id)` from `test`.`t1` where (`test`.`t1`.`id` > (/* select#2 */ select `test`.`t1`.`a` from `test`.`t1` limit 1))
drop tables t1, t2;
create table t1(a int not null, b char(10) not null);
create table t2(a int not null, b char(10) not null);
insert into t1 values (1, 'a'), (2, 'b'), (3, 'c'), (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'),(8,'h'),(0,'j');
insert into t2 values (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'), (1, 'a'), (2, 'b'), (7, 'g'),(0,'i');
analyze table t1,t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select * from t1 where exists(select * from t2 where t2.b=t1.b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Parallel execute (4 workers, test.t1)
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Start temporary
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Using where; End temporary; Using join buffer (hash join)
Warnings:
Note	1276	Field or reference 'test.t1.b' of SELECT #2 was resolved in SELECT #1
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` semi join (`test`.`t2`) where (`test`.`t1`.`b` = `test`.`t2`.`b`)
explain select * from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` where true
explain select count(*) from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where true
drop table t1,t2;
# view
create table small (id INTEGER not null, PRIMARY KEY (id));
create table big (id INTEGER not null, PRIMARY KEY (id));
insert into small VALUES (1), (2);
insert into big VALUES (1), (2), (3), (4);
create view small_view AS SELECT *, IF (id % 2 = 1, 1, 0) AS is_odd FROM small;
create view big_view AS SELECT big.*, small_view.id AS small_id, small_view.is_odd FROM big LEFT JOIN small_view ON small_view.id = big.id;
explain select count(*) from small_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	small	NULL	index	NULL	PRIMARY	4	NULL	2	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`small`
explain select count(*) from big_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	4	100.00	Parallel execute (4 workers, test.big)
1	SIMPLE	big	NULL	index	NULL	PRIMARY	4	NULL	4	100.00	Using index
1	SIMPLE	small	NULL	eq_ref	PRIMARY	PRIMARY	4	test.big.id	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`big` left join (`test`.`small`) on((`test`.`small`.`id` = `test`.`big`.`id`)) where true
drop table small, big;
drop view small_view, big_view;
# insert select
set pq_support_features_switch='insert_select=on';
create table t0 (a int);
create table t1 (a int);
insert into t0 values (1),(2),(3);
insert into t1 values (1),(2),(3);
explain insert into t1 select * from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	Parallel execute (4 workers, test.t0)
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select `test`.`t0`.`a` AS `a` from `test`.`t0`
explain insert into t1 select count(*) from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select count(0) AS `count(*)` from `test`.`t0`
drop table t0,t1;
# PQ_HINT
create table t1(a int primary key, b varchar(10), c decimal(5,2));
explain select/*+ PQ(2) */ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select/*+ PQ(2) */ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
drop table t1;
set session pq_support_features_switch='simple_aggregate=on';
# aggregate with single Query
create table t1(a int primary key, b varchar(10), c decimal(5,2));
create table t2(a int primary key, b varchar(10), c decimal(5,2));
insert into t1 values (1, 'The', '3.14');
insert into t1 values (2, 'quick', '4.25');
insert into t1 values (3, 'brown', '5.36');
insert into t1 values (4, 'fox', '6.47');
insert into t1 values (5, 'jumps', '3.14');
insert into t1 values (6, 'over', '4.25');
insert into t1 values (7, 'the', '5.36');
insert into t1 values (8, 'lazy', '6.47');
insert into t1 values (9, 'dog', '7.58');
insert into t2 select * from t1 where a > 3;
analyze table t1;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
analyze table t2;
Table	Op	Msg_type	Msg_text
test.t2	analyze	status	OK
explain select count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select count(*), sum(c) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)`,sum(`test`.`t1`.`c`) AS `sum(c)` from `test`.`t1`
explain select count(*) from t1 where c > 5.0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	33.33	Using where
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where (`test`.`t1`.`c` > 5.00)
explain select count(*) as cnt from t1 order by cnt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `cnt` from `test`.`t1`
explain select count(*) as g from t1 order by a limit 1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `g` from `test`.`t1` limit 1
explain select count(*) from t1 group by(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Using temporary
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`b`
explain select a, count(*) from t1 group by a order by b;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	9	100.00	Using temporary; Using filesort
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`a` order by `test`.`t1`.`b`
explain select count(*) from t1 join t2 on t1.a = t2.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t2)
1	SIMPLE	t2	NULL	index	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
1	SIMPLE	t1	NULL	eq_ref	PRIMARY	PRIMARY	4	test.t2.a	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` join `test`.`t2` where (`test`.`t1`.`a` = `test`.`t2`.`a`)
explain select distinct count(*) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1`
explain select a from t1 having a > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using where; Using index
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` having (`test`.`t1`.`a` > 3) order by `test`.`t1`.`a` limit 10
explain select sum(a+2) as SUM from t1 having SUM > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum((`test`.`t1`.`a` + 2)) AS `SUM` from `test`.`t1` having (`SUM` > 3)
explain select count(a) from t1 having sum(a) > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	9	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(a)` from `test`.`t1` having (sum(`<temporary>`.`sum(t1.a)`) > 3) limit 10
explain select sum(distinct t1.a) from t1 where t1.a > 3;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	range	PRIMARY	PRIMARY	4	NULL	6	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`a`) AS `sum(distinct t1.a)` from `test`.`t1` where (`test`.`t1`.`a` > 3)
drop table t1,t2;
# subquery
create table t1 (id int, a int);
create table t2 (b int);
insert into t1 values (1, 1), (2, 1), (0, 2), (1, 3), (3, 3), (0, 2);
insert into t2 values (1), (3), (2), (0), (1), (2), (1);
analyze table t1, t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select id, (select avg(b) from t2) as t from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,(/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`) AS `t` from `test`.`t1`
explain select sum(t1.a) from t1, t2 group by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t1, test.t2)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Using join buffer (hash join)
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `sum(t1.a)` from `test`.`t1` join `test`.`t2` group by `((select #2) + t1.a)`
explain select t1.a from t1 order by (select avg(t1.id) from t1) + t1.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using filesort
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` order by `((select #2) + t1.a)`
explain select * from t1 where a > (select avg(b) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a` from `test`.`t1` where (`test`.`t1`.`a` > (/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`))
explain select id, sum((select avg(b) from t2)) as t from t1 group by id order by id;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary; Using filesort
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,sum((/* select#2 */ select avg(`test`.`t2`.`b`) from `test`.`t2`)) AS `t` from `test`.`t1` group by `test`.`t1`.`id` order by `test`.`t1`.`id`
explain SELECT SUM(a), (SELECT SUM(b) FROM t2) AS scalar FROM t1 HAVING SUM(a) > scalar;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(`test`.`t1`.`a`) AS `SUM(a)`,(/* select#2 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `scalar` from `test`.`t1` having (sum(`<temporary>`.`sum(t1.a)`) > `scalar`)
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1  HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	<derived2>	NULL	system	NULL	NULL	NULL	NULL	1	100.00	NULL
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
2	DERIVED	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
3	SUBQUERY	<gather3>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
3	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1`
explain SELECT * from t1 JOIN (SELECT SUM(a) as c1, (SELECT sum(b) FROM t2) AS c2 FROM t1 group by t1.a HAVING SUM(a) > c2) as dt;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
1	SIMPLE	<derived2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using join buffer (hash join)
2	DERIVED	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Using temporary
3	SUBQUERY	<gather3>	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	Parallel execute (4 workers, test.t2)
3	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	7	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`id` AS `id`,`test`.`t1`.`a` AS `a`,`dt`.`c1` AS `c1`,`dt`.`c2` AS `c2` from `test`.`t1` join (/* select#2 */ select sum(`test`.`t1`.`a`) AS `c1`,(/* select#3 */ select sum(`test`.`t2`.`b`) from `test`.`t2`) AS `c2` from `test`.`t1` group by `test`.`t1`.`a` having (sum(`<temporary>`.`a`) > `c2`)) `dt`
explain select sum(distinct t1.id) from t1 where t1.id > (select t1.a from t1 limit 1);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	33.33	Using where
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	Parallel execute (4 workers, test.t1)
2	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	6	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`id`) AS `sum(distinct t1.id)` from `test`.`t1` where (`test`.`t1`.`id` > (/* select#2 */ select `test`.`t1`.`a` from `test`.`t1` limit 1))
drop tables t1, t2;
create table t1(a int not null, b char(10) not null);
create table t2(a int not null, b char(10) not null);
insert into t1 values (1, 'a'), (2, 'b'), (3, 'c'), (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'),(8,'h'),(0,'j');
insert into t2 values (3, 'c'), (4, 'd'), (5, 'f'), (6, 'e'), (1, 'a'), (2, 'b'), (7, 'g'),(0,'i');
analyze table t1,t2;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
test.t2	analyze	status	OK
explain select * from t1 where exists(select * from t2 where t2.b=t1.b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Parallel execute (4 workers, test.t1)
1	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Start temporary
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	11.11	Using where; End temporary; Using join buffer (hash join)
Warnings:
Note	1276	Field or reference 'test.t1.b' of SELECT #2 was resolved in SELECT #1
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` semi join (`test`.`t2`) where (`test`.`t1`.`b` = `test`.`t2`.`b`)
explain select * from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` where true
explain select count(*) from t1 where exists(select count(*) from t2);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	PRIMARY	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	9	100.00	NULL
2	SUBQUERY	<gather2>	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	Parallel execute (4 workers, test.t2)
2	SIMPLE	t2	NULL	ALL	NULL	NULL	NULL	NULL	8	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` where true
drop table t1,t2;
# view
create table small (id INTEGER not null, PRIMARY KEY (id));
create table big (id INTEGER not null, PRIMARY KEY (id));
insert into small VALUES (1), (2);
insert into big VALUES (1), (2), (3), (4);
create view small_view AS SELECT *, IF (id % 2 = 1, 1, 0) AS is_odd FROM small;
create view big_view AS SELECT big.*, small_view.id AS small_id, small_view.is_odd FROM big LEFT JOIN small_view ON small_view.id = big.id;
explain select count(*) from small_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	2	100.00	Parallel execute (4 workers, test.small)
1	SIMPLE	small	NULL	index	NULL	PRIMARY	4	NULL	2	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`small`
explain select count(*) from big_view;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	4	100.00	Parallel execute (4 workers, test.big)
1	SIMPLE	big	NULL	index	NULL	PRIMARY	4	NULL	4	100.00	Using index
1	SIMPLE	small	NULL	eq_ref	PRIMARY	PRIMARY	4	test.big.id	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`big` left join (`test`.`small`) on((`test`.`small`.`id` = `test`.`big`.`id`)) where true
drop table small, big;
drop view small_view, big_view;
# insert select
set pq_support_features_switch='insert_select=on';
create table t0 (a int);
create table t1 (a int);
insert into t0 values (1),(2),(3);
insert into t1 values (1),(2),(3);
explain insert into t1 select * from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	Parallel execute (4 workers, test.t0)
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select `test`.`t0`.`a` AS `a` from `test`.`t0`
explain insert into t1 select count(*) from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	INSERT	t1	NULL	ALL	NULL	NULL	NULL	NULL	NULL	NULL	NULL
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	Parallel execute (4 workers, test.t0)
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	3	100.00	NULL
Warnings:
Note	1003	insert into `test`.`t1` /* select#1 */ select count(0) AS `count(*)` from `test`.`t0`
drop table t0,t1;
# PQ_HINT
create table t1(a int primary key, b varchar(10), c decimal(5,2));
explain select/*+ PQ(2) */ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select/*+ PQ(2) */ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ sum(a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ sum(`test`.`t1`.`a`) AS `sum(a)` from `test`.`t1`
explain select /*+ PQ(t1 2)*/ a from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (2 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select /*+ PQ(`t1`@`select#1`) */ `test`.`t1`.`a` AS `a` from `test`.`t1`
drop table t1;
create table t1(a int primary key, b varchar(10), c decimal(5,2));
create table t2(a int primary key, b varchar(10), c decimal(5,2));
analyze table t1;
Table	Op	Msg_type	Msg_text
test.t1	analyze	status	OK
analyze table t2;
Table	Op	Msg_type	Msg_text
test.t2	analyze	status	OK
set optimizer_trace = 'enabled=on';
explain select a from t1 having a > 3 order by a limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	NULL	PRIMARY	4	NULL	1	100.00	Using where; Using index
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a` from `test`.`t1` having (`test`.`t1`.`a` > 3) order by `test`.`t1`.`a` limit 10
SELECT json_extract(trace,"$.steps[*].not_apply_pq_plan") FROM INFORMATION_SCHEMA.OPTIMIZER_TRACE;
json_extract(trace,"$.steps[*].not_apply_pq_plan")
NULL
explain select count(*) from t1 group by(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Using temporary
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` group by `test`.`t1`.`b`
SELECT json_extract(trace,"$.steps[*].not_apply_pq_plan") FROM INFORMATION_SCHEMA.OPTIMIZER_TRACE;
json_extract(trace,"$.steps[*].not_apply_pq_plan")
NULL
explain select count(*) from t1 join t2 on t1.a = t2.a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel execute (4 workers, test.t1)
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	1	100.00	Using index
1	SIMPLE	t2	NULL	eq_ref	PRIMARY	PRIMARY	4	test.t1.a	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select count(0) AS `count(*)` from `test`.`t1` join `test`.`t2` where (`test`.`t2`.`a` = `test`.`t1`.`a`)
SELECT json_extract(trace,"$.steps[*].not_apply_pq_plan") FROM INFORMATION_SCHEMA.OPTIMIZER_TRACE;
json_extract(trace,"$.steps[*].not_apply_pq_plan")
NULL
explain select sum(distinct t1.a) from t1;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	index	PRIMARY	PRIMARY	4	NULL	1	100.00	Using index
Warnings:
Note	1003	/* select#1 */ select sum(distinct `test`.`t1`.`a`) AS `sum(distinct t1.a)` from `test`.`t1`
SELECT json_extract(trace,"$.steps[*].not_apply_pq_plan") FROM INFORMATION_SCHEMA.OPTIMIZER_TRACE;
json_extract(trace,"$.steps[*].not_apply_pq_plan")
[{"chosen": false, "reason": "Query has unsupported scenarios including generated column, unsupported data type, unsupported function and so on.", "select#": 1}]
set optimizer_trace = 'enabled=off';
drop table t1,t2;
# 3. Test simple_aggregate optimization in pq_support_features_switch
set session sql_mode = '';
drop table if exists tb_pq_optimize_001;
Warnings:
Note	1051	Unknown table 'test.tb_pq_optimize_001'
create table tb_pq_optimize_001(id int, varchar_col varchar(255), blob_col blob, date_col date, text_col text, json_col json, bit_col bit);
insert into tb_pq_optimize_001 values (1, 'abcd', 'aaa', '2024-09-10', 'text type', '{\"key2\": 2}', 0x0), (2, 'wwwwwwwww', 'bbbbbbbbb', '2024-09-13', '*%^GGHJJ', '{\"key\": 1, \"key2\": 2}', 0xff), (3, 'aaaaaaaaaa', 'ccccccccccc', '2024-09-15','123mbhj', '{\"key2\": 2}', 0x11), (4, 'abababa', 'ddddddddddd', '2024-09-16', 'abcdabcd', '{\"key1\": 1}', 0x55), (5, 'dfghjkl', 'eeeeeeeeeeeeee', '2024-09-18', 'aaa', '{\"key1\": 1}', 0x1010);
Warnings:
Warning	1264	Out of range value for column 'bit_col' at row 2
Warning	1264	Out of range value for column 'bit_col' at row 3
Warning	1264	Out of range value for column 'bit_col' at row 4
Warning	1264	Out of range value for column 'bit_col' at row 5
analyze table tb_pq_optimize_001;
Table	Op	Msg_type	Msg_text
test.tb_pq_optimize_001	analyze	status	OK
set session force_parallel_execute=ON;
set session pq_support_features_switch='simple_aggregate=on,count_distinct=on';
show variables like "%force_parallel_execute%";
Variable_name	Value
force_parallel_execute	ON
show variables like "%pq_support_features_switch%";
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session force_parallel_execute=ON;
set session pq_support_features_switch='simple_aggregate=on,count_distinct=off';
show variables like "%force_parallel_execute%";
Variable_name	Value
force_parallel_execute	ON
show variables like "%pq_support_features_switch%";
Variable_name	Value
pq_support_features_switch	simple_aggregate=on,count_distinct=off,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session force_parallel_execute=ON;
set session pq_support_features_switch='simple_aggregate=off,count_distinct=on';
show variables like "%force_parallel_execute%";
Variable_name	Value
force_parallel_execute	ON
show variables like "%pq_support_features_switch%";
Variable_name	Value
pq_support_features_switch	simple_aggregate=off,count_distinct=on,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session force_parallel_execute=ON;
set session pq_support_features_switch='simple_aggregate=off,count_distinct=off';
show variables like "%force_parallel_execute%";
Variable_name	Value
force_parallel_execute	ON
show variables like "%pq_support_features_switch%";
Variable_name	Value
pq_support_features_switch	simple_aggregate=off,count_distinct=off,correlated_subquery=on,hash_join_spill_to_disk=on,insert_select=on
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session pq_support_features_switch='simple_aggregate=on,count_distinct=on';
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session pq_support_features_switch='simple_aggregate=on,count_distinct=off';
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session pq_support_features_switch='simple_aggregate=off,count_distinct=on';
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
set session pq_support_features_switch='simple_aggregate=off,count_distinct=off';
explain select id, date_col from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	Parallel execute (4 workers, test.tb_pq_optimize_001)
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`tb_pq_optimize_001`.`id` AS `id`,`test`.`tb_pq_optimize_001`.`date_col` AS `date_col` from `test`.`tb_pq_optimize_001`
explain select count(id) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(`test`.`tb_pq_optimize_001`.`id`) AS `count(id)` from `test`.`tb_pq_optimize_001`
explain select count(distinct date_col) from tb_pq_optimize_001;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	tb_pq_optimize_001	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select count(distinct `test`.`tb_pq_optimize_001`.`date_col`) AS `count(distinct date_col)` from `test`.`tb_pq_optimize_001`
drop table if exists tb_pq_optimize_001;
set global force_parallel_execute = @saved_force_parallel_execute;
SET pq_support_features_switch = @saved_pq_support_features_switch;
SET global pq_support_features_switch = @saved_pq_support_features_switch;
set session parallel_cost_threshold = @saved_parallel_cost_threshold;
set session parallel_rows_threshold = @saved_parallel_rows_threshold;
set session sql_mode = @saved_sql_mode;
