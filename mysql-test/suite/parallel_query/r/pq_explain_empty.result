DROP TABLE IF EXISTS t_empty;
CREATE TABLE t_empty (id INT PRIMARY KEY, val INT) ENGINE=InnoDB;
SET SESSION parallel_query=ON;
SET SESSION parallel_cost_threshold=0;
EXPLAIN SELECT * FROM t_empty;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t_empty	NULL	ALL	NULL	NULL	NULL	NULL	1	100.00	Parallel query eligible, execution disabled, serial fallback, dop=4
Warnings:
Note	1003	/* select#1 */ select `test`.`t_empty`.`id` AS `id`,`test`.`t_empty`.`val` AS `val` from `test`.`t_empty`
EXPLAIN FORMAT=TREE SELECT * FROM t_empty;
EXPLAIN
-> Table scan on t_empty, parallel query eligible, execution disabled, serial fallback (dop=4)  (cost=0.35 rows=1)

SET SESSION parallel_query=OFF;
SET SESSION parallel_cost_threshold=10000;
DROP TABLE t_empty;
