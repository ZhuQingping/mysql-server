CREATE TABLE `t4` (
`id` int NOT NULL AUTO_INCREMENT,
`a` int DEFAULT NULL,
`b` int DEFAULT NULL,
`c` varchar(100) DEFAULT NULL,
`d` varchar(100) DEFAULT NULL,
PRIMARY KEY (`id`),
KEY `idx_abc` (`a`,`b`,`c`)
) ENGINE=InnoDB AUTO_INCREMENT=16384 DEFAULT CHARSET=utf8mb4;
INSERT INTO `t4` VALUES
(1,1,2,'abcabc',    'cbacbacbacba'),
(2,3,2,'abcabca',   'cbacbacbacba'),
(3,1,2,'abca',      'cbacbacbacba'),
(4,1,2,'abcabca',   'cbacbacbacba'),
(5,1,1,'abcabcabc', 'cbacbacbacba'),
(6,0,3,'cabcabcabc','cbacbacbacba'),
(7,1,2,'abc',       'cbacbacbacba'),
(8,2,4,'abcabd',    'cbacbacbacba'),
(9,1,0,'xabcabcad', 'cbacbacbacba'),
(10,1,5,'xycabcab', 'cbacbacbacba');
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
INSERT INTO t4(a, b, c, d) SELECT a, b, c, d FROM t4;
SET GLOBAL innodb_stats_persistent_sample_pages = 2000;
ANALYZE TABLE t4;
Table	Op	Msg_type	Msg_text
test.t4	analyze	status	OK
SET GLOBAL innodb_stats_persistent_sample_pages = DEFAULT;
SELECT * FROM t4 where id > 1 and a < 1 and b = 2 limit 10;
id	a	b	c	d
SELECT * FROM t4 where (id  > 1 and a < 1) or (b = 2) limit 10;
id	a	b	c	d
1	1	2	abcabc	cbacbacbacba
2	3	2	abcabca	cbacbacbacba
3	1	2	abca	cbacbacbacba
4	1	2	abcabca	cbacbacbacba
6	0	3	cabcabcabc	cbacbacbacba
7	1	2	abc	cbacbacbacba
16384	1	2	abcabc	cbacbacbacba
16385	3	2	abcabca	cbacbacbacba
16386	1	2	abca	cbacbacbacba
16387	1	2	abcabca	cbacbacbacba
EXPLAIN SELECT * FROM t4 where id > 1 and a + b < 2 limit 10;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	10240	100.00	Parallel execute (4 workers, test.t4)
1	SIMPLE	t4	NULL	range	PRIMARY	PRIMARY	4	NULL	10240	100.00	Using where
Warnings:
Note	1003	/* select#1 */ select `test`.`t4`.`id` AS `id`,`test`.`t4`.`a` AS `a`,`test`.`t4`.`b` AS `b`,`test`.`t4`.`c` AS `c`,`test`.`t4`.`d` AS `d` from `test`.`t4` where ((`test`.`t4`.`id` > 1) and ((`test`.`t4`.`a` + `test`.`t4`.`b`) < 2)) limit 10
SELECT * FROM t4 where id > 1 and a + b < 2 limit 10;
id	a	b	c	d
9	1	0	xabcabcad	cbacbacbacba
16392	1	0	xabcabcad	cbacbacbacba
16407	1	0	xabcabcad	cbacbacbacba
16417	1	0	xabcabcad	cbacbacbacba
16438	1	0	xabcabcad	cbacbacbacba
16448	1	0	xabcabcad	cbacbacbacba
16458	1	0	xabcabcad	cbacbacbacba
16468	1	0	xabcabcad	cbacbacbacba
16501	1	0	xabcabcad	cbacbacbacba
16511	1	0	xabcabcad	cbacbacbacba
SELECT * FROM t4 where id < 10 or (a + b < 5) limit 10;
id	a	b	c	d
1	1	2	abcabc	cbacbacbacba
2	3	2	abcabca	cbacbacbacba
3	1	2	abca	cbacbacbacba
4	1	2	abcabca	cbacbacbacba
5	1	1	abcabcabc	cbacbacbacba
6	0	3	cabcabcabc	cbacbacbacba
7	1	2	abc	cbacbacbacba
8	2	4	abcabd	cbacbacbacba
9	1	0	xabcabcad	cbacbacbacba
16384	1	2	abcabc	cbacbacbacba
EXPLAIN SELECT SUM(a) FROM t# group by b HAVING b > # and SUM(a) + b > # order by b;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index; Using temporary; Using filesort
Warnings:
Note	####	/* select## */ select sum(`test`.`t#`.`a`) AS `SUM(a)` from `test`.`t#` group by `test`.`t#`.`b` having (`(t#.b > #)` and ((sum(`<temporary>`.`a`) + `test`.`t#`.`b`) > #)) order by `test`.`t#`.`b`
SELECT SUM(a) FROM t4 group by b HAVING b > 1 and SUM(a) + b > 5 order by b;
SUM(a)
14336
4096
2048
SELECT * FROM t4 where c < make_set(1, 'abca') and b > 1 limit 10;
id	a	b	c	d
7	1	2	abc	cbacbacbacba
16390	1	2	abc	cbacbacbacba
16405	1	2	abc	cbacbacbacba
16415	1	2	abc	cbacbacbacba
16436	1	2	abc	cbacbacbacba
16446	1	2	abc	cbacbacbacba
16456	1	2	abc	cbacbacbacba
16466	1	2	abc	cbacbacbacba
16499	1	2	abc	cbacbacbacba
16509	1	2	abc	cbacbacbacba
SELECT * FROM t4 where c < make_set(1 | 4, 'abc', 'bca', 'ccb') and a < 2 order by id limit 10;
id	a	b	c	d
7	1	2	abc	cbacbacbacba
16390	1	2	abc	cbacbacbacba
16405	1	2	abc	cbacbacbacba
16415	1	2	abc	cbacbacbacba
16436	1	2	abc	cbacbacbacba
16446	1	2	abc	cbacbacbacba
16456	1	2	abc	cbacbacbacba
16466	1	2	abc	cbacbacbacba
16499	1	2	abc	cbacbacbacba
16509	1	2	abc	cbacbacbacba
SELECT * FROM t4 where id IN (1, 3, 5) and b > 1;
id	a	b	c	d
1	1	2	abcabc	cbacbacbacba
3	1	2	abca	cbacbacbacba
SELECT * FROM t4 where id IN (1, 3, 5, 7, 9, 10) and (b > 1 or a < 3) and a + b > 2;
id	a	b	c	d
1	1	2	abcabc	cbacbacbacba
3	1	2	abca	cbacbacbacba
7	1	2	abc	cbacbacbacba
10	1	5	xycabcab	cbacbacbacba
EXPLAIN SELECT SUM(a), SUM(a)/AVG(b) from t# group by a HAVING AVG(b) + a > # order by a;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index
Warnings:
Note	####	/* select## */ select sum(`test`.`t#`.`a`) AS `SUM(a)`,(sum(`<temporary>`.`sum(t#.a)`) / avg(`<temporary>`.`avg(t#.b)`)) AS `SUM(a)/AVG(b)` from `test`.`t#` group by `test`.`t#`.`a` having ((avg(`<temporary>`.`avg(t#.b)`) + `test`.`t#`.`a`) > #) order by `test`.`t#`.`a`
SELECT SUM(a), SUM(a)/AVG(b) from t4 group by a HAVING AVG(b) + a > 2 order by a;
SUM(a)	SUM(a)/AVG(b)
0	0.0000
14336	7168.0000
4096	1024.0000
6144	3072.0000
SELECT SUM(a) from t4 where a + b < 3 and id < 100;
SUM(a)
2
SELECT SUM(b) from t4 where a * b < 3 or id > 1000;
SUM(b)
47093
EXPLAIN SELECT b, COUNT(a) from t# group by b HAVING b IN (#, #, #, #, #) and SUM(a) > #### ORDER BY SUM(b);
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index; Using temporary; Using filesort
Warnings:
Note	####	/* select## */ select `test`.`t#`.`b` AS `b`,count(`test`.`t#`.`a`) AS `COUNT(a)` from `test`.`t#` group by `test`.`t#`.`b` having (`(t#.b in (#,#,#,#,#))` and (sum(`<temporary>`.`a`) > ####)) order by sum(`<temporary>`.`b`)
SELECT b, COUNT(a) from t4 group by b HAVING b IN (1, 2, 3, 5, 7) and SUM(a) > 2000 ORDER BY SUM(b);
b	COUNT(a)
1	2048
5	2048
2	10240
SELECT SUM(a) FROM t4 where c = make_set(1, 'abcabc');
SUM(a)
2048
SELECT SUM(a), c FROM t4 group by b, c HAVING c < make_set(1 | 4, 'abc', NULL, 'a') and b > 1 order by b, c;
SUM(a)	c
2048	abc
SELECT SUM(a) FROM t4 group by make_set(1 | 2, 'ab', 'c');
SUM(a)
24576
explain select make_set(NULL,'a','b','c'),make_set(#|#,'a',NULL,'c'),make_set(#+#,'a',NULL,'c') from t# group by a, b HAVING sum(a)/(AVG(b) + #) > #;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index
Warnings:
Note	####	/* select## */ select make_set(NULL,'a','b','c') AS `make_set(NULL,'a','b','c')`,make_set((# | #),'a',NULL,'c') AS `make_set(#|#,'a',NULL,'c')`,make_set((# + #),'a',NULL,'c') AS `make_set(#+#,'a',NULL,'c')` from `test`.`t#` group by `test`.`t#`.`a`,`test`.`t#`.`b` having ((sum(`<temporary>`.`sum(t#.a)`) / (avg(`<temporary>`.`avg(t#.b)`) + #)) > #)
select make_set(NULL,'a','b','c'),make_set(1|4,'a',NULL,'c'),make_set(1+2,'a',NULL,'c') from t4 group by a, b HAVING sum(a)/(AVG(b) + 1) > 2;
make_set(NULL,'a','b','c')	make_set(1|4,'a',NULL,'c')	make_set(1+2,'a',NULL,'c')
NULL	a,c	a
NULL	a,c	a
NULL	a,c	a
NULL	a,c	a
NULL	a,c	a
NULL	a,c	a
select make_set(NULL,'a','b','c'), make_set(1+2,'a',NULL,'c') from t4 group by a, b HAVING sum(a) > 2 and b > 1;
make_set(NULL,'a','b','c')	make_set(1+2,'a',NULL,'c')
NULL	a
NULL	a
NULL	a
NULL	a
SELECT AVG(a) from t4 group by b, c HAVING c = make_set(1, 'abca') or b = 2 order by b, c;
AVG(a)
1.0000
1.0000
1.0000
2.0000
SELECT COUNT(a) from t4 group by b HAVING b IN (1, 2, 3, 4, 6) and SUM(b - 2) > 1 order by b;
COUNT(a)
2048
2048
SELECT COUNT(b IN (1, 3, 5)) from t4;
COUNT(b IN (1, 3, 5))
20480
EXPLAIN SELECT b as c#, SUM(a) from t# group by b HAVING c# in (#, #, #) or # * b < # and AVG(c#) > # order by c#;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index; Using temporary; Using filesort
Warnings:
Note	####	/* select## */ select `test`.`t#`.`b` AS `c#`,sum(`test`.`t#`.`a`) AS `SUM(a)` from `test`.`t#` group by `test`.`t#`.`b` having (`(c# in (#,#,#))` or (`((# * t#.b) < #)` and (avg(`<temporary>`.`avg(t#.b)`) > #))) order by `c#`
SELECT b as c1, SUM(a) from t4 group by b HAVING c1 in (1, 3, 5) or 2 * b < 5 and AVG(c1) > 1 order by c1;
c1	SUM(a)
1	2048
2	14336
3	0
5	2048
EXPLAIN SELECT b in (#, #, #) as c#, SUM(b) from t# group by c# HAVING c# IN (#, #) and AVG(b) > # order by c#;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
#	SIMPLE	<gather#>	NULL	ALL	NULL	NULL	NULL	NULL	#####	###.##	Parallel execute (# workers, test.t#)
#	SIMPLE	t#	NULL	index	idx_abc	idx_abc	###	NULL	#####	###.##	Using index; Using temporary; Using filesort
Warnings:
Note	####	/* select## */ select (`test`.`t#`.`b` in (#,#,#)) AS `c#`,sum(`test`.`t#`.`b`) AS `SUM(b)` from `test`.`t#` group by `c#` having (`(c# in (#,#))` and (avg(`<temporary>`.`avg(t#.b)`) > #)) order by `c#`
SELECT b in (1, 3, 5) as c1, SUM(b) from t4 group by c1 HAVING c1 IN (0, 1) and AVG(b) > 1 order by c1;
c1	SUM(b)
0	28672
1	18432
DROP TABLE t4;
