create table t(a int);
insert into t values(1),(2);
explain select a, sysdate() from t;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	<gather1>	NULL	ALL	NULL	NULL	NULL	NULL	2	100.00	Parallel execute (4 workers, test.t)
1	SIMPLE	t	NULL	ALL	NULL	NULL	NULL	NULL	2	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select `test`.`t`.`a` AS `a`,now() AS `sysdate()` from `test`.`t`
select a, sysdate() from t;
a	sysdate()
1	#
2	#
drop table t;
