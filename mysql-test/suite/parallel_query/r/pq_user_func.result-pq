create table t0(id int);
insert into t0 values(1),(2),(3),(4),(5);
explain select USER() from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select user() AS `USER()` from `test`.`t0`
explain select CURRENT_USER() from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select current_user() AS `CURRENT_USER()` from `test`.`t0`
explain select SESSION_USER() from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select user() AS `SESSION_USER()` from `test`.`t0`
explain select SYSTEM_USER() from t0;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t0	NULL	ALL	NULL	NULL	NULL	NULL	5	100.00	NULL
Warnings:
Note	1003	/* select#1 */ select user() AS `SYSTEM_USER()` from `test`.`t0`
select USER() from t0;
USER()
root@localhost
root@localhost
root@localhost
root@localhost
root@localhost
select CURRENT_USER() from t0;
CURRENT_USER()
root@localhost
root@localhost
root@localhost
root@localhost
root@localhost
select SESSION_USER() from t0;
SESSION_USER()
root@localhost
root@localhost
root@localhost
root@localhost
root@localhost
select SYSTEM_USER() from t0;
SYSTEM_USER()
root@localhost
root@localhost
root@localhost
root@localhost
root@localhost
DROP TABLE t0;
