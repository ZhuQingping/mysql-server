#
# Bug #56716 Dstore locks a record gap without locking the table
#
--source include/have_only_dstore.inc

CREATE TABLE bug56716 (a INT PRIMARY KEY,b INT,c INT,INDEX(b)) ENGINE=Dstore;

SELECT * FROM bug56716 WHERE b<=42 ORDER BY b DESC FOR UPDATE;

DROP TABLE bug56716;
