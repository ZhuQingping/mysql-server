
(values row(1));

SELECT (values row(1));

(values row(1), row(2));

--error ER_SUBQUERY_NO_1_ROW
SELECT (values row(1), row(2));

--error ER_SUBQUERY_NO_1_ROW
SELECT (values row(1), row(2)) union  select 1,2;

--error ER_SUBQUERY_NO_1_ROW
SELECT (values row(1), row(2)) union SELECT (values row(1), row(2));

SELECT 3 > ALL (VALUES ROW(1), ROW(2));

create table t0(a int);

SELECT * FROM t0 WHERE a >ALL (VALUES ROW(1), ROW(2));

drop table t0;


